// Dummy file to prevent build errors while printer functionality is disabled
class BtprintExecutor {
  void testPrint() async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printCheckinSlip(dynamic data) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printBill(dynamic data) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printReport(dynamic data) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> slipCheckin(dynamic data) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printInvoice(dynamic data) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printTransfer(dynamic bluetooth, dynamic data, int footerStyle) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printFooter(dynamic bt, dynamic ivc, dynamic roomType, int typeFooter, double? ppn, double? service) async {
    // Disabled - printer functionality temporarily unavailable
  }
  
  Future<void> printFnB(List<dynamic> data, dynamic bt) async {
    // Disabled - printer functionality temporarily unavailable
  }
}